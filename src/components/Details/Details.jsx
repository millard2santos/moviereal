const Details = ({ title, img, rating }) => {




    return (
        <div>
            <p>{title}</p>
            <p>{img}</p>
            <p>{rating}</p>
        </div>

    )
}

export default Details